<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zswltech.mithras.payment.mapper.PaymentPolicyInfoMapper">
    <select id="countPolicyCode" resultType="int">
        select count(1)
        from payment_policy_info p
                 INNER JOIN payment_base_info b on p.payment_id = b.id
        where p.policy_code = #{policyCode}
          and b.payment_status != 'CLOSED'
    </select>

    <select id="listPolicyCode" resultType="cn.zswltech.mithras.payment.mapper.model.PaymentPolicyInfo">
        select *
        from payment_policy_info p
        INNER JOIN payment_base_info b on p.payment_id = b.id
        where p.policy_code in
        <foreach collection="policyCodes" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        and b.payment_status != 'CLOSED'
    </select>

    <update id="occupyPolicy">
        update payment_policy_info
        set payment_id = #{paymentId}
        where contract_id = #{contractId}
          and payment_id is null
    </update>

    <update id="cancelOccupyPolicy">
        update payment_policy_info
        set payment_id = null
        where contract_id = #{contractId}
          and payment_id = #{paymentId}
    </update>
</mapper>
