# zswl-mithras-workbench

`zswl-mithras-workbench` 是工作台域模块，负责快捷入口、公告、卡片指标、柱状/雷达/小图表、工作台初始化和指标刷新任务。

本模块的核心语义是“用户进入系统后的工作入口和轻量聚合展示”。它可以展示多业务域数据，但不应承载核心业务规则。

后续整理重点是保持工作台只做入口与展示聚合，跨域数据由 application adapter 或明确接口提供。
