# sonarqube-check:
#     image: sonarsource/sonar-scanner-cli
#     variables:
#         SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
#         GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
#         SONAR_HOST_URL: "http://172.16.200.21:9000"
#         SONAR_LOGIN: "c71ed8b92139f15471b80e4ec47053a00f622c13"
#         SONAR_PROJECT_KEY: "cn.zswltech:mithras-react"
#         SONAR_PROJECT_NAME: "mithras-react"
#         BRANCH: "${GIT_BRANCH}"
#     cache:
#         key: "${CI_JOB_NAME}"
#         paths:
#             - .sonar/cache
#     script:
#       - sonar-scanner -X -Dsonar.sources=. -Dsonar.host.url=$SONAR_HOST_URL -Dsonar.login=$SONAR_LOGIN -Dsonar.password=$SONAR_PASSWORD  -Dsonar.projectKey=${SONAR_PROJECT_KEY} -Dsonar.projectName=${SONAR_PROJECT_NAME}
#     allow_failure: true

#  only:
#    - v1030
#    - master # or the name of your main branch
