<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zswltech.mithras.service.mapper.collection.CollectionOverdueRecordInfoMapper">


    <select id="getLastRecordByCollectionId" resultType="cn.zswltech.mithras.service.mapper.model.collection.CollectionOverdueRecordInfo">
        select id, max(record_date) as record_date
        from collection_overdue_record_info
        where collection_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </select>


</mapper>
