<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zswltech.mithras.service.mapper.budget.BudgetPlanPayDetailFtpInterestMapper">
    <select id="listCostGroupByDetail" resultType="cn.zswltech.mithras.service.mapper.dto.BudgetPlanPayDetailCostGroupDTO">
        select
            budget_plan_pay_detail_id as budgetPlanPayDetailId,
            sum(ifnull(cash_interest,0)) as costSum
        from budget_plan_pay_detail_ftp_interest
        where 1 = 1 and deleted = 0
        <if test="budgetPlanPayDetailIds != null and budgetPlanPayDetailIds.size() > 0">
            and budget_plan_pay_detail_id in
            <foreach collection="budgetPlanPayDetailIds" item="item" open="(" close=")" separator=",">
                #{item}
            </foreach>
        </if>
        <if test="startDate != null">
            and interest_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            and interest_date &lt;= #{endDate}
        </if>
        group by budget_plan_pay_detail_id
    </select>

    <select id="calculateCostByDetailId" resultType="long">
        select
        ifnull(sum(ifnull(cash_interest, 0)), 0)
        from
        budget_plan_pay_detail_ftp_interest
        where deleted = 0
        and budget_plan_pay_detail_id = #{detailId}
        <if test="startDate != null">
            and interest_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            and interest_date &lt;= #{endDate}
        </if>
    </select>
</mapper>