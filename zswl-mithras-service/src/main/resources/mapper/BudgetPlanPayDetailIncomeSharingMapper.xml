<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zswltech.mithras.service.mapper.budget.BudgetPlanPayDetailIncomeSharingMapper">
    <select id="listIncomeGroupByDetail" resultType="cn.zswltech.mithras.service.mapper.dto.BudgetPlanPayDetailIncomeGroupDTO">
        select
            budget_plan_pay_detail_id as budgetPlanPayDetailId,
            sum(ifnull(income,0)) as incomeSum
        from budget_plan_pay_detail_income_sharing
        where 1 = 1 and deleted = 0
        <if test="budgetPlanPayDetailIds != null and budgetPlanPayDetailIds.size() > 0">
            and budget_plan_pay_detail_id in
            <foreach collection="budgetPlanPayDetailIds" item="item" open="(" close=")" separator=",">
                #{item}
            </foreach>
        </if>
        <if test="startDate != null">
            and income_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            and income_date &lt;= #{endDate}
        </if>
        group by budget_plan_pay_detail_id
    </select>

    <select id="calculateIncomeByDetailId" resultType="long">
        select
            ifnull(sum(ifnull(income, 0)), 0)
        from
            budget_plan_pay_detail_income_sharing
        where deleted = 0
            and budget_plan_pay_detail_id = #{detailId}
        <if test="startDate != null">
            and income_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            and income_date &lt;= #{endDate}
        </if>
    </select>
</mapper>