# zswl-mithras-blackgray

`zswl-mithras-blackgray` 是黑灰名单域模块，负责黑灰名单库、规则配置、入库任务、失信名单、人工出库和黑灰名单相关审核。

本模块的核心语义是“对象是否命中黑灰名单及其名单生命周期”。它可以服务客户、风控、项目准入等场景，但不应承载这些业务域的完整规则。

当前模块 POM 低耦合，后续应继续保持独立，通过接口向其他域提供名单判断和名单事实。
