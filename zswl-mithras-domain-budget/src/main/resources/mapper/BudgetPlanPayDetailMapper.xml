<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zswltech.mithras.service.mapper.budget.BudgetPlanPayDetailMapper">
    <select id="calculatePrincipalBalance" resultType="long">
        select
            sum(ifnull(plan_pay_amount,0))
        from budget_plan_pay_detail
        where 1=1 and deleted = 0
        and id in
        <foreach collection="budgetPlanPayDetailIds" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
        and plan_pay_date &gt;= #{budgetWriteDateFrom}
        and plan_pay_date &lt; #{budgetDateFrom}
    </select>
</mapper>